import { CircularProgress } from "@mui/material"

export const ButtonCircularLoading = () => {
    return (
        <CircularProgress
            size={20}
            sx={{ color: '#081B4E' }}
        />
    )
}